const discord = require("discord.js");

module.exports.run = async (bot, message, args) => {
    return message.channel.send("düüd düüd düd lissen düd its not - yea düüd... IM DUNE xqcSlam");
}

module.exports.help = {
    name: ("xqc")
}